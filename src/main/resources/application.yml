spring:
  application:
    name: code-refactoring
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{50} - %msg%n"
server:
  port: 6500